# 跨角色協作清單

> 整理日期：2026-06-18｜撰寫：R7
> 列出目前各角色互相依賴的事項，供開會確認優先順序與時程。

---

## ✅ R2 決策已出（原阻塞項已解）

> 完整串接做法見 [`Auth-Firestore_串接指南_R2決議.md`](./Auth-Firestore_串接指南_R2決議.md)。

### R2 → R3

| 事項 | 決議 | 後續 |
|------|------|------|
| **對話紀錄儲存架構** | ✅ **Firestore**（B 級完整還原，存整包 result；不寫 polaris_core） | R2 立 Firestore + schema → R3 接 `/history`、`/subscriptions` |

### R2 → R7

| 事項 | 決議 | 後續 |
|------|------|------|
| **Auth 框架** | ✅ **NextAuth + Google OAuth**（後端驗 Google id_token，`sub` 主鍵） | R7 建 `[...nextauth]/route.ts`、帶 Bearer 呼叫後端 |
| **Magic Link** | ❌ **砍**（只 Google OAuth） | R7 移除設定頁 Magic Link UI |
| **Google OAuth 憑證** | 由 R2/PM 在 GCP Console 建 OAuth Client | 提供 `CLIENT_ID`/`CLIENT_SECRET`，設 redirect = `https://<vercel>/api/auth/callback/google` |
| ~~Magic Link 寄信服務~~ | 已不需要（Magic Link 砍） | — |

> 📄 完整規格見 [`R2_需求清單_from_R7.md`](./R2_需求清單_from_R7.md) 與 [`Auth-Firestore_串接指南_R2決議.md`](./Auth-Firestore_串接指南_R2決議.md)

---

## 🟡 R3 → R7（R3 交付後前端自動接上）

| 端點 | 對應前端功能 | 優先 | 狀態 |
|------|------------|------|------|
| `GET /alerts` 補 `origin` 欄位 | 研究助理 + 同業比較 監控警示面板 | 🔴 高 | ⏳ 欄位不完整 |
| `POST /contradiction` | 矛盾偵測（兩頁面，按鈕 + 自動觸發） | 🔴 高 | ⏳ 端點不存在 |
| `GET /chunk/{source_id}` | 引用追蹤器點開原文 | 🔴 高 | ⏳ 端點不存在 |
| PR #6 merge | 引用追蹤器文件標籤（doc_type / published_at） | 🟡 中 | ⏳ 待 merge |
| `GET /suggestions?mode=peer` | 同業比較頁快速提問 chip | 🟡 中 | ⏳ 前端 hook 已就緒，等後端 |
| `GET /subscriptions` | 頁面載入時取得已訂閱清單 | 🟡 中 | ⏳ 端點不存在 |
| `POST /subscriptions` + `GET /tracking-feed` | 訂閱設定儲存 + 追蹤通知顯示 | 🟡 中 | ⏳ 端點不存在 |
| `POST /research` 補 `chart` + `kpis` | 研究助理量化分析區塊（圖表 + KPI 卡） | 🟡 中 | ⏳ `normalizeResearch` 暫時 hardcode `[]` |
| `POST /peer-compare` | 同業比較整頁換成真實資料 | ⚪ 排工時 | ⏳ 端點不存在 |
| `POST /history` | 對話紀錄永久儲存 | ⚪ 等 R2 決策後 | ⏳ 前端已有 localStorage MVP |

> 📄 完整規格（request / response schema）見 [`docs/cross-role-collab/R3_需求清單_from_R7.md`](./R3_需求清單_from_R7.md)

---

## 🟡 R4 → R7（R4 交付後前端自動接上）

| 端點 | 對應前端功能 | 優先 | 狀態 |
|------|------------|------|------|
| `GET /library` | 資料庫頁顯示已入庫文件列表 | 🟡 中 | ⏳ 端點不存在；UI 已就緒；BQ `chunks` 已有 6,885 筆 |

> 📄 完整規格見 [`docs/cross-role-collab/R4_需求清單_from_R7.md`](./R4_需求清單_from_R7.md)

---

## 🟡 R4 → R3（資料層依賴）

| 事項 | 說明 |
|------|------|
| **法說稿 chunks 入庫完整度** | R3 的 HybridRetriever 品質取決於 R4 入庫數量；目前 20 檔 ticker、1,608 筆 transcript |
| **BQ schema / index 變更** | R3 若需新增欄位，走 SOP §7 PR，由 R4 執行 |

---

## 開會確認事項

> 格式：⏳ 待確認 ｜ ✅ 已決定（填入結論）

### 與 R2 確認：

| # | 問題 | 狀態 | 結論 |
|---|------|------|------|
| 1 | Auth 框架選哪套？（影響 R7 整個登入流程） | ✅ | **NextAuth + Google OAuth**；Magic Link 砍。串接指南見 [`Auth-Firestore_串接指南_R2決議.md`](./Auth-Firestore_串接指南_R2決議.md) |
| 2 | 對話紀錄存哪？（影響 R3 是否要實作 `POST /history`） | ✅ | **Firestore**（B 級完整還原）；history + 訂閱共用。R3 接 `/history`、`/subscriptions` |

### 與 R3 確認：

| # | 問題 | 狀態 | 結論 |
|---|------|------|------|
| 3 | PR #6 預計何時 merge？ | ⏳ | — |
| 4 | `GET /alerts` origin 欄位補齊的時程？（🔴 高優先，前端監控面板全空白） | ⏳ | — |
| 5 | `POST /contradiction` 的時程？ | ⏳ | — |

### 與 R4 確認：

| # | 問題 | 狀態 | 結論 |
|---|------|------|------|
| 6 | `GET /library` 端點排進哪一週？ | ⏳ | — |

---

*需求規格文件索引：*
- *[R2_需求清單_from_R7.md](./R2_需求清單_from_R7.md) — Auth + 對話紀錄儲存*
- *[R3_需求清單_from_R7.md](./R3_需求清單_from_R7.md) — 9 個 API 端點規格*
- *[R4_需求清單_from_R7.md](./R4_需求清單_from_R7.md) — GET /library*
