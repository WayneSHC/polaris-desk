# R7-1 前端落地待辦 ＋ 引用追蹤器 `/chunk` 契約

> 整理日期：2026-06-18｜對象：R7（前端落地）、R3（`/chunk` 端點）
> 本文把已拍板的決議收斂成「R7 要改什麼」＋「R3 要補什麼」，可直接動工。
> 完整串接做法見同資料夾 [`Auth-Firestore_串接指南_R2決議.md`](./Auth-Firestore_串接指南_R2決議.md)。

---

## 一、已拍板決議（recap）

| 項目 | 結論 |
|---|---|
| 登入 | **Google OAuth + NextAuth**；後端驗 Google id_token（`aud`=client_id、`sub` 當主鍵） |
| Magic Link | ❌ **砍**（只 Google OAuth） |
| 使用者紀錄 / 訂閱儲存 | **Firestore**（GCP 原生、不寫 `polaris_core`） |
| 紀錄深度 | **B 級完整還原**（存整包 `result`，點開還原當時答案，不重跑） |
| 紀錄版面 | 用**現有 `/history` 分頁**即可，**不做** Claude-Code 常駐側欄 |
| 時程 | 測試期就接、Demo 用登入版；無 token 匿名僅作 CI / 斷網降級 |
| 後端 | ✅ **已實作**（`auth.py`/`user_store.py`/`/history`/`/subscriptions`，token-free TDD）；Firestore DB 已建（asia-east1）、SA 已授權 |

---

## 二、R7 前端落地待辦（讀 `frontend-UI_2026_0618` 實況整理）

你的 app 已很完整（`/history` 分頁、grouped 今日/本週/更早都在）。要落地本需求需動 4 處：

1. **`/history` 由 A 級升 B 級**
   - 現況：點一筆帶 `?q=` **重跑**查詢，資料在 localStorage（`historyStore`）。
   - 改為：登入時讀後端 `GET /history` 列表 + `GET /history/{id}` **還原整包 `result`**（不重打）。
   - localStorage 退為**未登入 / 斷網**的本機 fallback。

2. **接 Google 登入**
   - 現況：設定頁 Google 按鈕**無 onClick**、帳號 hardcoded「Jing Chen / 已登入」。
   - 改為：`signIn("google")`、AppShell 顯示真實使用者、呼叫後端帶 `Authorization: Bearer <id_token>`（見指南 §3-3）。

3. **砍 Magic Link**
   - 現況：設定頁「以工作郵箱登入」表單**仍在**（`setSent` mock）。
   - 改為：移除該區塊（決議只做 Google OAuth）。

4. **寫入紀錄**
   - 研究 / 同業比較拿到結果後 `POST /history`（B 級：帶整包 `result`）。見指南 §3-4。

> 匿名降級不可破：無 token → `/history` 走 localStorage、`/ask`·`/research` 照常（保斷網備援）。

---

## 三、引用追蹤器 `GET /chunk/{source_id}` 契約（給 R3）

> 來源：R7 提供的「引用追蹤器」原型（HTML + 螢幕錄製，2026-06-18）。DocViewer 點引用 →
> 開原始文件、**逐字高亮**、顯示頁碼 / 可信度 / 「來源已驗證·未經改寫」。下列契約照原型欄位定死，
> R3 實作後前端 mock 換真**零重工**。

### Request
```
GET /chunk/{source_id}          # 需 viewer-scoped（chunks 有 owner/confidential 存取控制，同 /ask）
```

### Response
```jsonc
{
  "source_id": "stub-2330-2026Q1-fin",
  "title":      "台積電_2026Q1_合併財報.pdf",   // 文件顯示名
  "doc_type":   "transcript",                  // transcript|major_news|news…
  "kind_label": "財務報表",                     // doc_type→中文顯示（DocViewer 標題列）
  "ticker":     "2330",
  "fiscal_period": "2026Q1",
  "published_at":  "2026-04-18",
  "page":     "p.11",                          // 真實頁碼（見缺口①）
  "trust":    "high",                          // high | mid（可信度標籤；見缺口②）
  "content":  "完整段落原文…",                  // DocViewer 逐行顯示的原文 body
  "highlight":"毛利率 57.8%，營業利益率 47.5%…", // 被引用的片段（= 該 chunk snippet）
  "hl_tokens":["57.8","439,105","47.5"]        // 要高亮的字串（可選；無則前端用 highlight 比對）
}
```
查無 / viewer 無權 → **404**。

### 欄位對應原型
`title`←檔名 · `kind_label`←doc_type 標籤 · `page`←頁碼 · `trust`←可信度 · `content`←body · `highlight`←擷取片段 · `hl_tokens`←要高亮的 token。

### ⚠️ 兩個資料缺口（R3 + R4 要先確認）
1. **真實頁碼 `page`**：BQ `chunks` 文字片段目前**未必有 `page_num`**（整頁向量 `colpali_pages` 才有）。
   → R4 確認；若無，回 `null`，前端顯示「頁碼未提供」（DocViewer 仍可顯示原文 + 片段）。
2. **可信度 `trust` 規則**：現無此欄，需**定義**。建議：依 `doc_type` 權威性 + rerank 分數分箱（`high`/`mid`）；
   過渡期可先固定 `high`，但要 log 為暫定。

### 不變量
- `chunks` **不可前端裸查**（owner/confidential）→ 一律經此端點 viewer 過濾後回。
- 欄位名鎖定，改契約＝R3/R7（/R4）一起改。

---

## 四、誰做什麼（收斂）

| 待辦 | 主導 | 狀態 |
|---|---|---|
| `/history` A→B、接 signIn+Bearer、砍 Magic Link、`POST /history` | **R7** | 待做（後端已就緒） |
| `GET /chunk/{source_id}`（引用追蹤器吃真資料） | **R3**（+R4 資料） | 待做（契約如上） |
| OAuth Client（GCP Console）+ Vercel env | 真人（R2/PM + R7） | 待做（見指南 §1、真人 checklist） |
| Firestore + 後端端點 + JWT 驗證 | R2 | ✅ 已完成 |
